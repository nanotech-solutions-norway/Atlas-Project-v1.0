# Persistent Context Rail

Purpose: preserve the visitor’s selected use case, saved proof items, and current next action across Atlas pages.

## Launch behavior
- Desktop: sticky side rail or structured inline context card
- Mobile: collapsible bottom sheet or docked action bar
- Must remain lightweight and non-sensitive
- Must not store secure runtime state in Wix
